# v0.0.6
- fix: added some jump instr handlers
## New instrs 
0x40, `or` - bitwise OR 
0x41, `and` - bitwise AND 
0x42, `xor` - bitwise XOR
0x43, `not` - bitwise inversion
0x44, `lnot` - logical NOT 
0x45, `nz` - nonzero  
